# =============================================================================
# Configuration de Base de Données - Bsissa
# =============================================================================
# Copiez ces lignes dans votre fichier .env à la racine du projet
# et modifiez les valeurs selon votre environnement
# =============================================================================

# -----------------------------------------------------------------------------
# Configuration MySQL Locale (Développement)
# -----------------------------------------------------------------------------
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=bsissa
DB_USERNAME=root
DB_PASSWORD=

# -----------------------------------------------------------------------------
# Configuration MySQL Production (Exemple)
# -----------------------------------------------------------------------------
# DB_CONNECTION=mysql
# DB_HOST=your-production-host.com
# DB_PORT=3306
# DB_DATABASE=bsissa_prod
# DB_USERNAME=bsissa_user
# DB_PASSWORD=votre_mot_de_passe_securise

# -----------------------------------------------------------------------------
# Configuration Railway (Exemple)
# -----------------------------------------------------------------------------
# DB_CONNECTION=mysql
# DB_HOST=${MYSQLHOST}
# DB_PORT=${MYSQLPORT}
# DB_DATABASE=${MYSQLDATABASE}
# DB_USERNAME=${MYSQLUSER}
# DB_PASSWORD=${MYSQLPASSWORD}

# -----------------------------------------------------------------------------
# Options de Configuration Additionnelles
# -----------------------------------------------------------------------------

# Activer le mode strict SQL (recommandé)
DB_STRICT_MODE=true

# Préfixe des tables (laisser vide par défaut)
DB_PREFIX=

# Timezone de la base de données
DB_TIMEZONE=+01:00

# Options de connexion
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# Pool de connexions (pour production)
DB_POOL_MIN=2
DB_POOL_MAX=10

# -----------------------------------------------------------------------------
# Notes Importantes
# -----------------------------------------------------------------------------
#
# 1. SÉCURITÉ : Ne committez JAMAIS le fichier .env avec vos vrais identifiants
# 2. Le fichier .env est dans .gitignore par défaut
# 3. Pour la production, utilisez des mots de passe forts et complexes
# 4. Vérifiez que l'utilisateur MySQL a les privilèges nécessaires :
#    - SELECT, INSERT, UPDATE, DELETE
#    - CREATE, ALTER, DROP (pour les migrations)
#    - INDEX, REFERENCES (pour les clés étrangères)
#
# Pour créer un utilisateur MySQL dédié :
#
#   CREATE USER 'bsissa_user'@'localhost' IDENTIFIED BY 'mot_de_passe_fort';
#   GRANT ALL PRIVILEGES ON bsissa.* TO 'bsissa_user'@'localhost';
#   FLUSH PRIVILEGES;
#
# -----------------------------------------------------------------------------
