#!/usr/bin/env php
<?php

/**
 * API Key Generator
 *
 * Génère des clés API sécurisées pour l'application Bsissa
 * Usage: php generate-api-key.php [length]
 */

function generateApiKey($length = 64, $prefix = '')
{
    // Méthode 1: random_bytes (le plus sécurisé)
    $bytes = random_bytes($length / 2);
    $key = bin2hex($bytes);

    return $prefix ? $prefix . '_' . $key : $key;
}

function generateUuid()
{
    // Génère un UUID v4
    $data = random_bytes(16);

    $data[6] = chr(ord($data[6]) & 0x0f | 0x40);
    $data[8] = chr(ord($data[8]) & 0x3f | 0x80);

    return vsprintf('%s%s-%s-%s-%s-%s%s%s', str_split(bin2hex($data), 4));
}

function generateBase64Key($length = 32)
{
    return rtrim(strtr(base64_encode(random_bytes($length)), '+/', '-_'), '=');
}

echo "═══════════════════════════════════════════════════════════════\n";
echo "              🔐 Générateur de Clés API - Bsissa\n";
echo "═══════════════════════════════════════════════════════════════\n\n";

// Obtenir la longueur depuis les arguments
$length = isset($argv[1]) ? (int)$argv[1] : 64;
if ($length < 32) {
    echo "⚠️  Avertissement: Longueur minimale recommandée: 32 caractères\n\n";
    $length = 32;
}

echo "Longueur de la clé: {$length} caractères\n\n";

// Générer différents types de clés
echo "┌─────────────────────────────────────────────────────────────┐\n";
echo "│ Type 1: Clé Hexadécimale (recommandé pour API)             │\n";
echo "└─────────────────────────────────────────────────────────────┘\n";
$hexKey = generateApiKey($length);
echo "API_KEY={$hexKey}\n\n";

echo "┌─────────────────────────────────────────────────────────────┐\n";
echo "│ Type 2: Clé Hexadécimale avec Préfixe                      │\n";
echo "└─────────────────────────────────────────────────────────────┘\n";
$prefixedKey = generateApiKey($length, 'bsissa');
echo "API_KEY={$prefixedKey}\n\n";

echo "┌─────────────────────────────────────────────────────────────┐\n";
echo "│ Type 3: Clé Base64 URL-Safe                                │\n";
echo "└─────────────────────────────────────────────────────────────┘\n";
$base64Key = generateBase64Key($length / 2);
echo "API_KEY={$base64Key}\n\n";

echo "┌─────────────────────────────────────────────────────────────┐\n";
echo "│ Type 4: UUID v4 (Standard)                                 │\n";
echo "└─────────────────────────────────────────────────────────────┘\n";
$uuid = generateUuid();
echo "API_KEY={$uuid}\n\n";

echo "┌─────────────────────────────────────────────────────────────┐\n";
echo "│ Type 5: UUID v4 sans tirets                                │\n";
echo "└─────────────────────────────────────────────────────────────┘\n";
$uuidNoDashes = str_replace('-', '', generateUuid());
echo "API_KEY={$uuidNoDashes}\n\n";

echo "┌─────────────────────────────────────────────────────────────┐\n";
echo "│ Type 6: Clé Laravel Style (base64:...)                     │\n";
echo "└─────────────────────────────────────────────────────────────┘\n";
$laravelKey = 'base64:' . base64_encode(random_bytes(32));
echo "API_KEY={$laravelKey}\n\n";

echo "═══════════════════════════════════════════════════════════════\n";
echo "                    💡 RECOMMANDATIONS\n";
echo "═══════════════════════════════════════════════════════════════\n\n";
echo "✓ Stockez la clé dans le fichier .env\n";
echo "✓ Ne committez JAMAIS la clé dans git\n";
echo "✓ Utilisez différentes clés pour dev/staging/prod\n";
echo "✓ Régénérez les clés périodiquement\n";
echo "✓ Longueur minimale recommandée: 32 caractères\n";
echo "✓ Longueur optimale: 64 caractères\n\n";

echo "═══════════════════════════════════════════════════════════════\n";
echo "               📝 UTILISATION DANS .env\n";
echo "═══════════════════════════════════════════════════════════════\n\n";
echo "Ajoutez cette ligne dans votre fichier .env:\n\n";
echo "API_KEY={$hexKey}\n\n";
echo "Ou pour Railway/Production:\n\n";
echo "export API_KEY=\"{$hexKey}\"\n\n";

echo "═══════════════════════════════════════════════════════════════\n\n";
